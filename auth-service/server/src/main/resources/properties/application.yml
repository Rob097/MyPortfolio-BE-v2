spring:
  config:
    import: classpath:common.yml
  datasource:
    username: 'myportfolio'
    url: jdbc:mysql://localhost:33061/auth
    password: 'password'
  zipkin:
    base-url: http://localhost:9411

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true